const WELCOME = 'welcome'
const FAVORITES = 'favorites'
const CATEGORIES = 'categories'

const PODCASTS = {
  VIEW: 'podcasts-view'
}

export default {
  WELCOME,
  PODCASTS,
  FAVORITES,
  CATEGORIES
}
